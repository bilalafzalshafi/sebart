## Detailed comments
 * Page #1: "real data," -- might indicate how many real datasets you consider? there's also a chance that if the ALE plots work, you can mention that you have some posterior summarization strategies to interpret the regression effects

 * Page #1 (Introduction): The initial BART also works for classification. There should be a version for multinomial outcomes too, so those don't require Gaussian errors

 * Page #1 (Introduction): There's also a paper by Murray and then one by me and Canale that adapt BART for count responses

 * Page #2 (Introduction):
   > Copula-process BART methods (Wenkel et al., 2026) derive a copula from the BART posterior and transport it through a chosen marginal to obtain distributional predictions, but they still depend on that external marginal rather than data-driven learning

   you'll need more about this and DR BART. Those should be in a standalone paragraph about related methods that seek to go beyond traditional (Gaussian) BART

 * Page #2 (Introduction): It's probably a good idea to discuss transformations somewhere. I'm not sure where. Note that there are transformed Gaussian process papers too (see my paper with Wu for some references).

 * Page #2 (Introduction): "arbitrary response distributions" -- worth noting at appropriate places that we're thinking about *continuous* outcomes. SeBR can be adapted to discrete (e.g., count or rounded data) and semi-continuous (e.g., zero-inflated by otherwise positive data) data, but those papers are in draft form right now

 * Page #2 (Introduction): "three real datasets" -- Have you looked into other datasets? It's great that you have solid performance for these 3, but I wonder how much harder it would be to expand to 10-15 datasets

 * Page #3 (Model): "= m (cid:88) j=1 g(xi; Tj, Mj)" -- you've overloaded notation for Y *and* for g(). you'll need a different notation for g() i think. you also want to merge this with (1), i.e., that it follows (1) with g = identity and f_\theta(x_i) = BART formulation. That way you can reference (1)-(2) as SeBART while also noting how BART is a special case

 * Page #3 (Model): "Yi" -- don't need to switch notation like this

 * Page #3 (Model): Where are all the BART details? there needs to be more about BART. It also has many prior parameters and pre-processing steps and those need to be discussed and handled within the context of the transformed model

 * Page #4 (Model): "Transformation model and priors." -- This needs a little more background. For instance, it's possible to put a prior on g() directly (like a monotone GP) but that won't be convenient for sampling (i.e., the MCMC will mix very slowly). We did something like that in my paper with Canale for discrete data. Another example is this paper: https://projecteuclid.org/journalArticle/Download?urlId=10.1214%2F25-BA1554

 * Page #4 (Model): "p(θ) p(x) dθ dx." -- This is fine as a general definition, but later you'll need to be clear that our estimate of F_Z uses *posterior* (and data-driven) quantities for these terms, not prior quantities

 * Page #4 (Model): "A nonparametric prior on FY" -- might give more of a teaser here:  you can put a BNP prior on F_Y, learn it directly (i.e., regardless of the model for Z), and then use (3) to link it to the latent regression model

 * Page #4 (Model): this also enforces monotonicity by construction

 * Page #4 (Model): "We obtain draws of the posterior marginal FY |Dn" -- This is out of place. It's not really part of the model, but rather the algorithm. So the sampling steps should all go together--BART plus g. And g() has two steps, one for F_Y and one for estimating Fz

 * Page #4 (Model): "Dn" -- i think conditioning on y (or y_{1:n}, however you want to define it) is more correct here because it doesn't actually condition on all data D_n (i.e., the covariates are not involved)

 * Page #4 (Model): "Identifiability" -- identifiability needs to be clarified in the initial statement of the model. Then, in the algorithm section, we can describe the location-scale adjustment

 * Page #5 (Model): you can add the predictive distribution here as well, so the LHS is joint over (y*, g, theta) and the RHS has an extra term for p(y* | D, g, theta)

 * Page #5 (Model):
   > For posterior inference, we replace these population quantities with their posterior counterparts

   Well, sort of. This is *already* posterior inference because you have a posterior sampling algorithm described above! The part that you need to modify is the treatment of F_z

 * Page #6 (Model):
   > Following Kowal and Wu (2024), we note that this Bayesian-bootstrap update treats the yi's as iid and therefore employs a surrogate likelihood for g that omits a correction factor ω(y | g) arising from the regression model. The exact posterior would weight draws of g by ω(y | g), but the surrogate yields consistent transformation draws and avoids additional computation. If a fully corrected posterior is desired, importance sampling provides a simple fix: draw {g(s), θ(s)} from the surrogate sampler and reweight or resample using ω{y | g(s)} to recover expectations under the exact likelihood. Empirically the adjustment has minimal impact even for modest n, so we rely on the surrogate sampler for efficiency

   I think we can shorten this and not bring so much attention to it. We're targeting the posterior for F_Y, so it's reasonable to say that the information content is mostly going to come from the y's themselves (not via the x's in the latent regression model). We can note that it's possible to adjust the posterior for F_Y to account for the latent regression (2) via importance sampling, but typically this makes little difference (and cite my paper with wu).

 * Page #7 (Approximating FZ): not sure what this part is getting at

 * Page #7 (Posterior prediction): i suppose it's fine to have this as its own section, but then you need to note that earlier. i commented above about the predictive distribution because your intro to the model/sampler talks about prediction, but then only deals with posterior inference

 * Page #8 (Identifiability and robustness): This is part of the model, so it needs to be described there

 * Page #9 (Identifiability and robustness): I'm not sure that the recentering steps really matter in the pilot run, as long as you have an intercept and scale in the full sampling algorithm

 * Page #10 (Design): "record results with n ∈ {150, 250, 300, 500}." -- and where are those?

 * Page #10 (Competing methods):
   > the root mean squared errors for sigmoid, beta, and arctangent responses

   What do you mean RMSEs? for what quantity? coefficients, regression function, out-of-sample prediction...?

 * Page #10 (Competing methods): "6.4, 0.25, and 77.7," -- this is not in the right spot. if it's purely for benchmarking and just to print out a few numbers (But not include in any plots), then include it in the results

 * Page #11 (Competing methods): "transformation (BART-BC), described below," -- why is this out of order then?

 * Page #11 (Competing methods): Some of this could be described in the introduction if you prefer, since we'll need more details on DR-BART

 * Page #12 (Competing methods): probably doesn't need it's own line

 * Page #12 (Competing methods): can also refer to the sampling steps in some algorithm you've already described for SeBART, since they should be the same

 * Page #12 (Evaluation metrics): "3.3 Evaluation metrics" -- i would just fold this into the next section of results, since it's pretty short

 * Page #12 (Evaluation metrics):
   > empirical coverage and mean width for posterior predictive intervals at the 80%, 90%, and 95% nominal levels using both equal-tail quantiles intervals.

   are these out-of-sample?

 * Page #12 (Evaluation metrics): "root mean squared error (RMSE) on held-out test observations" -- based on what? e.g., are you taking the posterior predictive expectation for each method?

 * Page #12 (Evaluation metrics):
   > We also record time taken for ecah model to train on a simulated dataset.

   and how many draws, burn-in, etc. are you doing?

 * Page #12 (Evaluation metrics): "ecah" -- typo

 * Page #12 (Results): you can mention the other intervals you considered here, and that those results are in the supplement (are they?)

 * Page #12 (Results): "equal-tail results" -- did you look at HPD intervals? If so, you might note that the conclusions are the same (are they?).

 * Page #13 (Results): how about with varying n?

 * Page #13 (Results): Did you also check a Box-Cox version? E.g., something like log- or sqrt- would be fair (and might be worse for DR-BART!). Identity too!

 * Page #14 (Results): "est = 200" -- not what was said before...

 * Page #15 (Competing methods): why not all of them?

 * Page #16 (Results): "80%, 90%, and 95%" -- no...

 * Page #17 (Results):
   > Figures 4 provide ECDF overlays for SeBART and BART respectively for all three datasets

   This isn't described clearly enough. You'll need to say what you're doing and why. Did you try in-sample ECDF and predictive ECDF comparisons? I can't tell what might be wrong here without knowing more

 * Page #17 (Results): These still don't look right

 * Page #18 (Results): "Accumulated Local Effects." -- This would really be part of the methods, and more details are needed. What are you doing and why? What's the general context for ALE? How does SeBART adapt to that?

 * Page #18 (Results):
   > all covariates: spatial coordinates (X, Y ) of the park grid, fine fuel moisture code (FFMC), Duff moisture code (DMC), drought code (DC), initial spread index (ISI), temperature, relative humidity (RH), wind speed, and rainfall

   those names should appear in the plot, not just X1, X2,...

 * Page #18 (Results): "D" -- remove

 * Page #18 (Results):
   > For SeBART, m(x) is obtained by averaging the model's posterior predictive mean after applying the learned transformation back to the observed scale.

   not quite. You can either take the posterior predictive expectation itself (not sure what the extra "averaging" refers to) or you can write E(y | x) \approx g^{1}{ f(x) } based on a first-order taylor expansion

 * Page #19 (Results): What does sblm find, especially in the case where sblm performs best?

 * Page #19 (Results): simulated data results should go in the simulated data section. Are these "correct"? what is the ground truth? E.g., what if you apply ALE to the true regression function?

